# README

This is a very old school stack. When updating styling, please update ONLY the `.scss` files. The entry file is `src\style\scss\main.scss`. The.css files can be regenerated using the command in the root README.md file.