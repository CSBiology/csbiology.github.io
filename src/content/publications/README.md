# README

## Rules

- You MUST not duplicate .bib files in `publications` and `publications/featured`. A given publication must be in one or the other, but not both. If you want to feature a publication, put it in `publications/featured` and remove it from `publications`.
- If you want to change a featured publication to a non-featured publication, move the .bib file from `publications/featured` to `publications`.